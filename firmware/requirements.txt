# MQTT communication
paho-mqtt>=2.1.0
python-dotenv>=1.0.1

# Presence detection (vision). opencv-python ships the DNN + HOG detectors and
# a VideoCapture fallback for dev on a laptop webcam. On Raspberry Pi this
# installs from piwheels automatically.
opencv-python>=4.8.0
numpy>=1.24.0

# Pi Camera capture. picamera2 is best installed via apt on Raspberry Pi OS:
#   sudo apt install -y python3-picamera2
# (pip install is fragile on Pi). Without it, capture falls back to OpenCV.
# picamera2

# Buzzer + stove SG90 servo GPIO control on the Raspberry Pi 4B. The firmware falls
# back to a simulated (no-op, logs only) backend automatically on a dev machine,
# so these are optional off-Pi (the markers skip them on non-ARM hosts).
#
# gpiozero 2.x on Raspberry Pi OS (Bookworm) selects the lgpio pin factory by
# default on the Pi 4B, so lgpio is the backend to install. (RPi.GPIO is the
# legacy backend and is no longer needed.) Pi 4B runs 64-bit (aarch64) or
# 32-bit (armv7l) Raspberry Pi OS — lgpio has wheels for both.
gpiozero>=2.0
lgpio>=0.2.2.0 ; platform_machine == "aarch64" or platform_machine == "armv7l"
