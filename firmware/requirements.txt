# MQTT communication
paho-mqtt>=2.1.0
python-dotenv>=1.0.1

# Presence detection (vision). opencv-python ships the DNN + HOG detectors and
# a VideoCapture fallback for dev on a laptop webcam. On Raspberry Pi this
# installs from piwheels automatically.
opencv-python>=4.8.0
numpy>=1.24.0

# Pi Camera capture. picamera2 is best installed via apt on Raspberry Pi OS:
#   sudo apt install -y python3-picamera2
# (pip install is fragile on Pi). Without it, capture falls back to OpenCV.
# picamera2

# --- Future feature dependencies (uncomment as you implement) ---
# gpiozero         # buzzer + relay GPIO control
# RPi.GPIO         # low-level GPIO backend for gpiozero
